# Windows RC1 PC acceptance — 2026-09-21

**Windows prerelease candidate, not stable 1.0. Owner authorized pushing and publishing the final reviewed packages on 2026-09-21.**

Source: latest main `b4d1b9b6c3cb40a512e49dad8d845856ed776e4d`, VERSION `1.0.0-rc.1`. The checkout was clean before a fast-forward update; existing workspace files were preserved. Preparation and UI revisions were kept local until the owner explicitly approved publication. The approved source changes and this report accompany the Windows RC1 release. Existing Windows Beta 4 and macOS releases were not changed.

## Packages

| File | SHA-256 |
|---|---|
| MuScriptor-Local-1.0-RC1-Windows-x64-Setup.exe | `b91566e61a12d613fb630b7b36a9c6b93fb91204bf00abdc5988921ea8c75715` |
| MuScriptor-Local-1.0-RC1-Windows-x64-Portable.zip | `82ebeafd207efb185e7e92e3e049d0ef396999073b5db0beafcf4f7d48f1e2d6` |

Both packages are unsigned. No publisher-signing identity was available. Portable ZIP integrity and private-content checks passed; no credentials, user recordings, local logs or personal home paths were found. Bundled worker/bootstrap/requirements match the checkout. Existing official upstream example audio and third-party notices are included.

## Build and local fixes

The matching [Windows Actions run](https://github.com/PokestirVGM/MuScriptor-Local/actions/runs/35663887750) built RC1 and passed Windows UI checks at 100%, 150% and 200%, browser state tests, packaged and installed startup, CPU bootstrap, worker/options/rhythm/session tests. It failed the subprocess shutdown test because the child could not import `worker`; artifact upload and the later DirectML checks did not run.

The Windows RC1 source includes these build and validation fixes:

- Give the shutdown-test child the worker source directory as its working directory, and include stderr in failure diagnostics. All eight Finish/cancellation tests then passed.
- Stage engine sources without `__pycache__`, `.pyc` or `.pyo` files in the Windows build. The first local package audit detected personal build paths in validation-generated bytecode; those packages were rejected. The final packages contain a clean source copy, leaving the working engine/cache intact.

Used the existing Windows build script with its pinned UI dependencies. The local build environment needed pip and Inno Setup 6.7.3; compiler binaries had valid Pyrsys B.V. Authenticode signatures. The deeply nested workspace exceeded an installer input-path limit, so the same built application was copied to a shorter staging path and compiled with the unchanged installer script. The final portable and installer use that identical clean application payload. No transcription, Mac or web behavior was changed locally.

## Windows UI revision after owner review

The packages above supersede the earlier local RC1 candidate with the requested Windows-only UI polish. Dropdown fields now use a single rounded border and a scalable chevron; opened lists use matching light/dark colors. The redundant App menu bar was removed. Its Repair Dependencies and Show Logs actions remain in the cog, and Windows instructions now point there.

The native Windows UI suite passed all 33 tests at each of 100%, 150% and 200% scaling. Light/dark renders were reviewed, including advanced timing controls. Keyboard selection and clicking the dropdown arrow to open/select from the menu passed in the Qt offscreen renderer with Windows fonts loaded. Native popup interaction could not be verified in this restricted session: even an unmodified Qt combo immediately hides its popup here. The owner subsequently reviewed installed UI revisions and approved publication; this does not establish the unavailable clean-profile checks listed below.

Rebuilt the Windows launcher using the existing build script's PyInstaller configuration and unchanged engine/frontend sources, then used the unchanged Inno Setup script at a shorter staging path. Reused the complete license notices from the preceding audited candidate because all dependency versions are identical. Packaged startup, ZIP integrity, source matching, and private-content audit passed. The existing transcription evidence below applies to the unchanged engine; GPU/DAW validation was not repeated for this UI-only change. No Mac or web files were modified.

### Follow-up: open dropdown lists

The owner clarified that the remaining issue was the open popup list on every dropdown. Replaced Qt's combo-specific menu delegate with a standard styled list delegate and removed the extra popup frame. Open lists now have one rounded border, flat rows and a rounded blue selection highlight in both themes. The current packages include this follow-up.

At the owner's request, validation was limited to focused light/dark popup rendering and keyboard/arrow-click selection in the offscreen Qt renderer, packaged startup, and the archive/privacy/hash checks. These passed. The earlier 33-test runs above precede this small popup-only follow-up; the full suite was not repeated. The owner subsequently supplied installed UI feedback and approved the final revision for publication.

### Follow-up: square backing behind rounded menus

Clipped each native popup window to its rounded list bounds and disabled the native popup shadow. This removes the square outer backing visible above/behind the styled list in the owner's latest screenshot. The current hashes identify this updated build. Focused native Qt checks confirmed the window mask excludes the corners/backing while retaining the list contents. Light/dark offscreen popup selection and packaged startup passed; no broad regression rerun. Archive/privacy checks passed. The owner approved this final revision for publication.

## Passed on this PC

- Packaged RC1 executable startup smoke test exited successfully.
- NVIDIA RTX 5070 Ti, PyTorch `2.7.1+cu128`, cached Large model: a complete real GPU transcription of a generated 12-second melody produced 20 notes without full CPU fallback.
- Finish here at the first completed section produced `_partial.mid` ending at five seconds. The saved partial session restored five seconds of audio. Retrying the same source transcribed all 12 seconds, not the partial cache.
- Native Qt integration with a real CUDA worker: cancel during model work, stop the old worker, retain file/timing, retry successfully, save a session, restart the engine and restore recovery.
- Desktop → local web HTTP session import → change timing to 140 BPM/6/8 → export/save recovery → Return to Desktop restored the newer web timing. Applying timing worked after return. Return and Quit closed the local servers. This checked real worker/server integration; browser state behavior was also covered by its regression suite.
- BPM 80/140/200 and 4/4/6/8 preserved all tested MIDI note starts/releases within **0.027 ms** of original timing. Explicit Strict quantization moved notes; disabling it restored original performance timing.
- Full local Python regression: **160 tests, 158 passed and two optional skips**. Browser state suite: **18 passed**.
- Separate pinned DirectML environment: **12 adapter tests passed**, including physical tiny-model token comparison against CPU on both RTX 5070 Ti and integrated AMD Radeon graphics. This is not a full RC1 transcription validation on RX 6800 XT.

## Blocked or not performed

- **Local installer/upgrade acceptance:** attempted the final installer; it exited before installation because the restricted execution session could not resolve the Windows `localappdata` shell folder (`SHGetFolderPath`, 0x80070002). The owner subsequently ran UI candidates and supplied screenshots, then approved RC1 publication. No separate clean-profile or detailed upgrade-retention result was recorded. This is distinct from the matching Actions run's successful installed startup and the passing engine-swap/rollback regression tests. The failed local attempt did not establish clean-profile installation, credential/cache retention or SmartScreen behavior.
- **DAW import/listening:** skipped at the owner's explicit request. MIDI serialization checks do not establish DAW tempo-map import, audible alignment or fractional-pickup handling.
- **Signing:** no Windows publisher certificate; no new macOS signing/notarization check on this PC.
- **Hardware/model coverage:** no RX 6800 XT hardware available here, no new complete DirectML RC1 run, no Small/Medium model acceptance and no clean-machine first-use model authentication/download acceptance. AMD remains experimental.
- **Other acceptance:** no real Mac-created session supplied; shared session interoperability was tested locally. Exhaustive native theme/scaling acceptance, model-download cancellation, optional MuseScore/FluidSynth/SoundFont renders and downloaded-package SmartScreen acceptance remain open.

The complex-music Auto tempo/meter limitations in FINAL_READINESS.md and METER_EVIDENCE.md still apply. Passing timing preservation does not mean automatic tempo/meter alignment is universally accurate. Keep RC1 prerelease labeling. Explicit owner approval to push and publish was received on 2026-09-21; existing Beta 4 and macOS releases must remain intact.
